{
    "name": "enmaboya/country-select",
    "description": "A Laravel Nova field.",
    "keywords": [
        "laravel",
        "nova"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1.0"
    },
    "autoload": {
        "psr-4": {
            "Enmaboya\\CountrySelect\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Enmaboya\\CountrySelect\\FieldServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
